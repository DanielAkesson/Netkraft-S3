A Networking library mainly design for unity. The whole library uses UDP socket but has support for Reliable UDP, Acknolaged UDP and many other message channels.
The libarary includes its own serlaizer and deserlaizer that in client agnostic.
The project has a small test suit that you can check out if your curious how the library works.
